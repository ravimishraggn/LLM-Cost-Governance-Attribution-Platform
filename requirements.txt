# --- Core gateway (Phase 1) ---
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy>=2.0
python-dotenv>=1.0
pyyaml>=6.0          # pricing config (Phase 2)

# --- Provider SDKs (lazy-imported; only needed for live calls) ---
openai>=1.30
anthropic>=0.28
boto3>=1.34          # AWS Bedrock

# --- Observability (Phase 3) ---
langfuse>=2.36

# --- Dashboard (Phase 5) ---
streamlit>=1.36
pandas>=2.2
plotly>=5.22

# --- Dev / test ---
pytest>=8.2
httpx>=0.27
